<div class="container">
  <h1><%="Welcome #{current_user.username}"%></h1>
</div>

<div class="container">
  <p>score</p>
  <%= @score %>
  <p>Lifelines</p>
  <%= @lifeline_count %>
</div>

<div class="container">
  <h2>Leaderboard</h2>

  <ul>
    <% @users.each do |user| %>
      <li><%= user.username %></li>
    <% end %>
  </ul>
</div>

<div class="container">
  <%= link_to "PLAY", root_path%>
</div>
